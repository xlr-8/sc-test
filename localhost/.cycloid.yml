version: "2"
name: localhost
canonical: localhost
author: cycloidio
status: private
description: localhost
image: ""
keywords:
- localhost
technologies: []
dependencies: []
config:
  default:
    description: ""
    name: default
    pipeline:
      pipeline:
        path: default/pipeline/terraform-pipeline.yaml
      variables:
        destination: ($ project $)/pipeline/variables-($ environment $).yml
        path: default/pipeline/variables.sample.yaml
    terraform:
      localhost:
        destination: ($ project $)/default/terraform/($ environment $)/localhost.tf
        path: default/terraform/localhost.tf.sample
